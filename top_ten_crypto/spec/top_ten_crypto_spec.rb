RSpec.describe TopTenCrypto do
  it "has a version number" do
    expect(TopTenCrypto::VERSION).not_to be "0.1.0"
  end

  it "does something useful" do
    expect(false).to eq(true)
  end

  it "scrapes page and returns
end
